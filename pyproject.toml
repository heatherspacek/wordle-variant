[project]
name = "wordle-variant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "textual-serve>=1.1.3",
]

[dependency-groups]
dev = [
    "py-spy>=0.4.1",
    "tqdm>=4.67.3",
]
